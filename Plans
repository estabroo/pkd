n-depth last seen succesful knocks to elminate replays (early or late check?)
different identifiers (PKD0 default) so multiple checks of the exact same thing don't have to happen for different keys
add ip as an option to the packet
use tcp packets and well known ports for firewall bypassing
add config file reading to knock program
c# version of knock for windows use
perl version of knock

ini style config

[name]
host=name_or_ip
key=blah
